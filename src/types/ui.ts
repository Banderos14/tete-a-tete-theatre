/** Тема оформления. Переключается атрибутом data-theme на <html>. */
export type Theme = 'dark' | 'light';

/** Фазы вступительной анимации занавеса на лендинге. */
export type IntroState = 'closed' | 'opening' | 'done';
