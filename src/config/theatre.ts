// Вместимость зала объявлена один раз — в серверном каталоге спектаклей,
// потому что авторитетная проверка вместимости происходит на сервере.
// Здесь только реэкспорт, чтобы фронтенд не мог разъехаться с сервером.
export { THEATRE_CAPACITY } from '../../api/_lib/shows';
