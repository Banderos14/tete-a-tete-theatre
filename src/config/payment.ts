// Реквизиты и контакты театра живут в shared/config/payment.ts: их читают и
// кабинет, и письма, которые теперь собирает сервер. Этот файл — прежний адрес
// импорта для фронтенда.
export * from '../../shared/config/payment';
